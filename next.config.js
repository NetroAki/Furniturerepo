/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // i18n will be handled through App Router middleware
  // Future: add middleware for locale detection and routing
};

module.exports = nextConfig;
